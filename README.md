# Intersport RAM

## Development

Install dependencies:

```bash
npm install
```

Run development server:

```bash
npm run dev
```

### Host on network

Run development server and host to local network:

```bash
npm run host
```